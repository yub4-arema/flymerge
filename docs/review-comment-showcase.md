# FlyMerge review comment showcase

This harmless documentation change exists to exercise the 🪰 report on a real pull request.
The runtime worker remains a tiny example, so the fly can inspect a meaningful signal.
This workflow check is intentionally boring and safe.
The example data stays local.
The data pipeline keeps the runtime worker and async stream tiny.
